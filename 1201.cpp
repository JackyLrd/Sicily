#incldue <stdio.h>
#include <string.h>
int main(void)
{
	char a[81],b[81],c[81];
	int T,n,i,j,k,l1,l2,str1[81],str2[81];
	scanf("%d",&T);
	for(i=1;i<=T;i++)
	{
		scanf("%s %s",&a,&b);
		if(strlen(a)>=strlen(b))
		{
			l1=strlen(a);
			l2=strlen(b);
		}
		else
		{
			l1=strlen(b);
			l2=strlen(a);
		}
        for(j=l2;j>=0;j--)
        {
        	if(j==l2)
        	{
        		
			}
		}
	}
}
