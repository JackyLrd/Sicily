#include <stdio.h>
#include <string.h>
int main(void)
{
	char str[5000],out[5000];
	int n,i,j,k,m,len;
	while(scanf("%s",&str)&&(str[0]!='X'||str[1]!='X'||str[2]!='X'))
	{
		len=strlen(str);
		for()
	}
}
